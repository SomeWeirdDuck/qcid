import 'package:flutter/material.dart';

class Blank_p extends StatelessWidget {
  const Blank_p({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("blank"),
      ),
      body: const Text("Still in development"),
    );
  }
}
